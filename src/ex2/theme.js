// import { createContext } from "react";

const theme = {
  light: {
    button: {
      foreground: "#000000",
      background: "#eeeeee"
    }
  },
  dark: {
    button: {
      foreground: "#ffffff",
      background: "#222222"
    }
  }
};

// const ThemeContext = createContext(theme);
export default theme;
